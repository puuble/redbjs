import { redis } from '../redis/client';

export async function up() {
  // Example: You could seed default data here if needed
  console.log('✅ users table assumed to be defined via schema registry');
}
